# C09/C10: selection hierarchy sensitivity and seed dispersion

Freeze this protocol before the new analysis. Use the existing six pairs, all three base seeds, both directions, original five relation-stratified original-triple folds (seed 20260901), 13-feature balanced logistic fit, original 0.05 ranking grid, and cached unfiltered features/RRs. No scorer or base-model training runs. No new TEST policy application. Existing TEST rows are read only in a separate descriptive seed-report phase after the DEV receipt is complete.

## C09 estimand and sensitivity

The original outer learner selects Global on outer training rows, fits median/scaler/logistic on its non-tied training rows, and selects beta/tau from the same outer training portion using fitted (resubstitution) preferences. Only the outer holdout evaluates it. This is an evaluable training algorithm; resubstitution selection within outer training does not itself leak outer-holdout labels. Final full-DEV fitting follows that same recipe with more data and its recorded random state. Original script text saying nested-selection does not mean an inner-CV search existed.

The outer estimate does not evaluate an entire adaptive research process if held-out OOF outcomes guided feature/action family changes. Reuse the bound development timeline: original ADC radius maximum .20, then P3 .50 and feature/fallback ablations, later matched families after recorded TEST exposure. Exact numbers of human inspection/iteration cycles and their causal influence remain unknown. Report this as retrospective fixed-family evaluation, not a fresh confirmation. Original-triple grouping keeps six seed/direction rows together; it does not make graph triples, shared entities or repeated observable queries independent. Quantify observable-query overlap between outer train/holdout without calling it an ID or feature leak.

For each original outer training portion, add three inner grouped folds with relation-stratified SHA256 assignment, fixed fold seed 2026091209. Each inner training portion independently selects its anchor and fits its preprocessing/classifier (same settings); each inner holdout scores all original 40 (beta=.05,.10,...,.50; tau=0,.1,.2,.3) configurations. Aggregate held-out RR sums with row-count weighting. Select using the original exact MRR tie rule: smaller beta, then larger tau. Transfer only selected beta/tau to the original outer-training anchor/classifier and evaluate the untouched outer holdout. Repeat the inner procedure on full DEV and report its selection without applying it to TEST. Fit random state is 20260902 + outer_index*100 + inner_index, with outer_index 1..5 and 0 for full DEV; indices for inner fits are 1..3. Every inner fold retains all six observations per original triple.

Report original resubstitution-40 and inner-OOF-40 in every fold and pooled DEV. Also evaluate explicit Global as a 41st candidate with the same exact rule, preferring beta=0 on an exact MRR tie; report resubstitution-41 and inner-OOF-41 separately so a benefit from adding Global is not confused with an inner-validation benefit. These are fixed diagnostic variants, not selection of a new action family. No repeated inner seeds or fold-count search. Expected additional fits: 6 pairs * (5 outer scopes + 1 full-DEV scope) * 3 = 108 small logistic models. Reuse and replay the 30 existing balanced outer fits and six final preference outputs; do not silently refit them under another sklearn version.

Report MRR, utility versus the appropriate outer anchor, fold choices, selected-training MRR, nonzero action fraction, mean absolute coefficient change, unconditional RR loss and harm. Pair inner-minus-original differences before an original-triple cluster bootstrap (2,000 resamples, RNG seed 2026091210 + pair index). Keep all six rows together. These are conditional intervals for fixed folds/models/choices, not corrected for method development, overlapping training folds, model fitting or multiple comparisons. More elaborate nested resampling is not required to define the original outer estimand.

## C10 denominators

For every one of the 76 available TEST method/pair cells in the complete main comparison, recover three absolute base-seed MRRs, both direction means and all six joint cells, plus mean and sample SD (ddof=1) over the three seed MRRs. R3 first averages its three selector repetitions within each base-seed/direction/query, as in the main MRR table; this is not nine independent base seeds. All three-seed means must reproduce the existing pooled MRR. Compute ADC-minus-each-comparator within seed before reporting its SD; never subtract marginal SDs. Verify existing paired point estimates.

Display all methods' absolute seed MRRs in the formal supplement. A compact six-pair table places each seed's ADC-minus-Global effect and effect mean +/- sample SD beside the already bound 10,000 original-triple conditional query interval. Seed SD describes dispersion of the observed checkpoint pairs on the same query set; it is not a standard error, a training-population confidence interval or an independent-selector estimate. ADC uses one pooled selector across seeds. Direction/cell win counts are descriptive, not independent replications. No claim that three seeds invalidate the study, and no substitution of new seeds or deletion of negative seeds.
