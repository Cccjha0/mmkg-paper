# D07/D09/D10 fixed outcome-risk diagnostic protocol

Written before this analysis on 2026-09-12. Use the six retained pairs, three base seeds and both directions, original grouped DEV holdouts and original TEST locks. Reuse the hash-bound unfiltered-feature ranking grids and saved OOF states; replay deployed actions and RR, fit no model, score no candidates and select no new policy. All diagnostics are retrospective and gold-aware evaluation quantities, never selector inputs.

## Populations and decomposition

Reference is each original policy's Global: the corresponding outer-training anchor on DEV, full-DEV anchor on TEST. Let d=RR_ADC−RR_Global and A indicate that the final projected weight differs from the anchor. Distinguish A from nonzero raw proposals, passing the confidence gate, and a change in the gold rank. Count all evaluated seed/direction/triple observations, not unique query keys. Assert exact sign masks match the established 1e−12 masks on these finite exported ranks; assert d=0 for every inactive observation. Report N, n_A, n_h, n_b, n_0, active unchanged count and supporting triple counts. Both P(harm)=n_h/N and P(harm|A)=n_h/n_A must have their own counts and conditional query intervals.

For every pair/split and every seed/direction cell compute H=n_h/N, B=n_b/N, Z=n_0/N, C=mean(−d|d<0), G=mean(d|d>0), L_minus=mean(max(−d,0)), L_plus=mean(max(d,0)) and U=mean(d). Verify B+H+Z=1, H*C=L_minus, B*G=L_plus, B*G−H*C=U at full precision before any display rounding. Empty conditional means/probabilities are undefined, not zero; empty unconditional contributions are zero. Keep all six pairs and both splits.

## Fixed tail diagnostics

Describe the loss magnitude −d among harmed observations using NumPy linear quantiles at .50/.90/.95/.99, maximum, and the share of total loss contributed by the largest ceil(.10*n_h) harmful observations. No quantile threshold is used to select a policy. Report predefined events −d>=.10 and −d>=.50, with counts/rates among all observations and actual interventions. Evaluate these two boundaries by the equivalent integer-rank cross-products (10 or 2 times the rank difference versus the rank product), avoiding rounding a mathematical .10 loss such as rank6→15 below the threshold. All magnitude means/decompositions otherwise use the full-precision exported RR values. These are absolute RR-unit thresholds: .50 includes a 1→2 change; .10 is a smaller but interpretable absolute loss (RR is in [0,1]), not a calibrated acceptability boundary.

Recover positive integer filtered ranks from reciprocal ranks and verify their reciprocals within 1e−12. Among observations with reference gold rank 1, report count, retained-rank-1 count/rate, rank-1 loss count/rate, and count/rate moving to rank>10; separately report these rates within reference-rank-1 observations that actually change weight. The diagnostic is filtered gold Hits@1 retention, not an unfiltered returned-list or end-user acceptance metric. Also report new rank-1 successes so net Hits@1 changes can be reconciled. Select exactly the three largest RR-loss examples per pair on TEST by (loss descending, seed, direction, h,r,t), retaining all if fewer than three. Publish IDs, filtered ranks, RR change and weights. They illustrate the predefined worst observed tail, not representative cases or independently validated deployment failures.

## Intervals and evidence

For the two harm populations, action/benefit/unchanged frequencies, conditional and unconditional means, net U, severe-event rates and rank-1 rates use 10,000 original-triple bootstrap resamples, preserving all six seed/direction observations. Resample the full triple universe *before* restricting to A, harmful, beneficial or reference-top-1 subsets; recompute numerators and denominators on each draw. Empty-denominator draws are excluded only for that metric and counted. Report the full and contributing cluster counts as well as valid replicate counts. Do not bootstrap only the selected intervention subset and do not apply Wilson/normal intervals to dependent rows.

Use the same lexicographic triple ordering and PCG64 dataset seeds as the unified C12 analysis: TEST 2026091212/2026091213 (MKG-W/DB15K); DEV 2026091214/2026091215. All metrics/pairs within a dataset and split use the same draws; ratios are never formed by dividing CI endpoints. Percentiles use linear interpolation. Reconcile TEST U and its interval with C12, and all available point counts/means with the B15/B16 audit. Tail quantiles and worst examples are descriptive sample summaries without added uncertainty intervals.

Intervals condition on the fitted models, observed seeds, fixed policies and DEV fold assignments. They omit fitting/selection uncertainty, graph/query dependence beyond triple clustering and historical developer adaptation; they are exploratory, pointwise and unadjusted for multiplicity. A zero observed event or a degenerate empirical-bootstrap interval is not a population safety guarantee. No claim comparing ADC's tail with another deployable policy will be made from this ADC-versus-Global diagnostic alone. Preserve old results and receipts. Store only small summaries, 72 seed/direction cells, 18 predetermined examples, source hashes and this protocol; no large per-query export or bootstrap matrix.
