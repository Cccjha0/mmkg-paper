# MMKG Paper 首轮开发任务清单（按脚本逐项勾选）

> 目标：完成首轮 **routing / regime / oracle** 分析闭环，为论文中的 RQ1 / RQ2 / RQ3 补齐最关键的新实验。  
> 当前仓库已经具备主模型 3-seed 结果、router 主叙事、threshold scan、feature ablation、subgroup / relation-group 分析，因此首轮开发重点应从“补模型训练”转向“构建统一 query-level 分析管线” :contentReference[oaicite:0]{index=0}  
> 同时，后续所有新增分析必须继续遵守论文当前已写明的两条 evaluation line 分离原则，以及 dev-only router training / test-only final reporting 的协议约束 :contentReference[oaicite:1]{index=1}

---

## 0. 首轮开发范围

本轮只做以下两类内容：

### A. 核心数据与评估管线
- query-level outcome 导出
- relation prior 构建
- router feature table 构建
- router 训练
- threshold scan 评估
- unified master table 构建

### B. 论文最关键新增分析
- E1. Per-regime win-rate
- E2. Per-regime oracle upper bound
- E6. Per-regime routing behavior
- E7. Oracle gap decomposition

> 暂不纳入首轮：
> - E3 XGBoost feature importance
> - E4 Logistic top coefficients
> - E5 δ scan summary
> - 统一论文绘图脚本美化
>
> 这些可以作为第二轮解释性增强内容。

> 备注：当前仓库中首轮主链路已有一版可运行实现，但部分中间产物的**路径、命名和格式**与本清单最初约定并不完全一致。例如，若某项能力已经由现有 `csv` 管线等价实现并跑通，则对应“开发任务/能力项”会按现状勾选；但若清单中写死的目标文件仍是 `parquet` 或指定目录下的另一命名形式，则该“输出文件”条目会继续保持未勾，以明确区分“能力已具备”和“文件契约完全对齐”这两件事。

---

## 1. 首轮开发顺序

- [x] Step 1. 导出 `Gate-only / Residual-only / Full Model` 的 query-level outcomes
- [x] Step 2. 基于 dev outcomes 构建 relation priors
- [x] Step 3. 构建 router feature tables（dev / test）
- [x] Step 4. 训练首轮 router（优先 XGBoost，保留 Logistic 接口）
- [x] Step 5. 跑 threshold scan，复现当前主稿中的 routing 主表和 threshold 行为
- [x] Step 6. 构建统一 `router_master_test` 分析底表
- [x] Step 7. 输出 E1 / E2 / E6 / E7 四张分析表
- [ ] Step 8. 检查结果能否直接嵌入论文第 5 节和第 6 节

---

# 2. 脚本级开发清单

---

## 2.1 `scripts/export_query_level_outcomes.py`

### 目标
为 `Gate-only`、`Residual-only`、`Full Model` 导出统一格式的 query-level outcome 表，作为后续所有 routing / regime 分析的底层输入。

### 依赖
- 已有模型 run 目录
- 各 run 下可读取 test / dev 的 query 级评估结果或可复算 rank / RR / score / margin 的数据

### 开发任务
- [x] 明确输入接口：`--run-dir --split --model-name --evaluation-line --out`
- [x] 支持 `split ∈ {dev, test}`
- [x] 支持 `model-name ∈ {gate_only, residual_only, full_model}`
- [x] 支持 `evaluation-line ∈ {routing_compatible, official}`
- [x] 从 run 中读取 query 级 rank 结果
- [x] 统一生成 `query_id`
- [x] 导出 `direction`
- [x] 导出 `relation_id`
- [x] 导出 `target_entity_id`
- [x] 导出 `target_has_img`
- [x] 导出 `target_regime`
- [x] 导出 `rank`
- [x] 导出 `rr`
- [x] 导出 `correct_score`
- [x] 导出 `top1_score`
- [x] 导出 `top2_score`
- [x] 导出 `margin = top1_score - top2_score`
- [x] 导出 `source_run_dir`
- [x] 导出 `source_seed`
- [x] 输出 parquet 文件到 `outputs/router/raw/`

### 输出文件
- [x] `outputs/router/raw/dev_gate_only_query_outcomes.parquet`
- [x] `outputs/router/raw/test_gate_only_query_outcomes.parquet`
- [x] `outputs/router/raw/dev_residual_only_query_outcomes.parquet`
- [x] `outputs/router/raw/test_residual_only_query_outcomes.parquet`
- [x] `outputs/router/raw/test_full_model_query_outcomes.parquet`

### 验收标准
- [x] 三个模型均能成功导出
- [x] `query_id` 在同一 split 内唯一
- [x] `target_regime` 只出现：
  - [x] `head_has_img`
  - [x] `head_no_img`
  - [x] `tail_no_img`
- [x] `rr = 1 / rank`
- [x] `margin >= 0`
- [x] 不混淆 official line 与 routing-compatible line

---

## 2.2 `scripts/build_relation_priors.py`

### 目标
只基于 **dev-side Gate-only / Residual-only** 的 query outcomes 构造 relation-level prior，供 router feature 使用。

### 依赖
- `dev_gate_only_query_outcomes.parquet`
- `dev_residual_only_query_outcomes.parquet`

### 开发任务
- [x] 明确输入接口：`--gate-dev --residual-dev --out`
- [x] 对齐两张 dev outcome 表的 `query_id`
- [x] 按 `relation_id` 聚合
- [x] 计算 `support`
- [x] 计算 `fusion_mean_rr`
- [x] 计算 `struct_mean_rr`
- [x] 计算 `mean_rr_gain`
- [x] 计算 `fusion_win_rate`
- [x] 计算 `struct_win_rate`
- [x] 计算 `tie_rate`
- [x] 生成 `is_visual_prior`
- [x] 输出 CSV 到 `outputs/router/raw/`

### 输出文件
- [x] `outputs/router/raw/dev_relation_priors.csv`

### 验收标准
- [x] 仅使用 dev 数据
- [x] 每个 `relation_id` 一行
- [x] `fusion_win_rate + struct_win_rate + tie_rate = 1`
- [x] `support` 与 relation query 数一致
- [x] 结果可直接 join 到 router feature table

---

## 2.3 `scripts/build_router_feature_table.py`

### 目标
构建 router 训练 / 推理所需的 feature tables，并保持与论文当前 feature 定义一致。

### 依赖
- `dev_gate_only_query_outcomes.parquet`
- `dev_residual_only_query_outcomes.parquet`
- `test_gate_only_query_outcomes.parquet`
- `test_residual_only_query_outcomes.parquet`
- `dev_relation_priors.csv`

### 开发任务
- [x] 明确输入接口：`--gate-dev --residual-dev --gate-test --residual-test --relation-priors --out-dir`
- [x] 构建 `router_features_dev.parquet`
- [x] 构建 `router_features_test.parquet`
- [x] 构建 `router_eval_targets_test.parquet`
- [x] 加入论文已定义的 feature：
  - [x] `direction`
  - [x] `target_has_img`
  - [x] `target_regime`
  - [x] `relation_id`
  - [x] `relation_gain_prior`
  - [x] `relation_fusion_win_rate`
  - [x] `relation_support`
  - [x] `relation_is_visual_prior`
  - [x] `text_img_cosine`
  - [x] `img_is_missing_replaced`
  - [x] `fusion_margin`
  - [x] `struct_margin`
  - [x] `fusion_correct_score`
  - [x] `struct_correct_score`
  - [x] `delta_margin`
- [x] 对 dev 表生成 label：
  - [x] `gain_label_d0`
  - [x] `gain_label_d001`
  - [x] `gain_label_d002`
- [x] 对 test 表默认不直接输出训练标签，避免误用
- [x] 保留 `rr_gate / rr_residual` 以供后续分析

### 输出文件
- [x] `outputs/router/features/router_features_dev.parquet`
- [x] `outputs/router/features/router_features_test.parquet`
- [x] `outputs/router/features/router_eval_targets_test.parquet`

### 验收标准
- [ ] 字段命名与论文 Table 3 一致
- [x] dev 表含 label，test 表不含训练标签
- [x] 无 test leakage 到 relation prior
- [x] `delta_margin = fusion_margin - struct_margin`
- [x] 可直接供 `train_router.py` 使用

---

## 2.4 `scripts/train_router.py`

### 目标
基于 dev feature table 训练 router。首轮优先保证 XGBoost 跑通，同时保留 Logistic 统一接口。

### 依赖
- `router_features_dev.parquet`

### 开发任务
- [x] 明确输入接口：`--train-table --model-type --feature-set --delta --out-dir`
- [x] 支持 `model-type ∈ {xgb, logistic}`
- [x] 支持 `feature-set ∈ {F1, F2, F3, F4, FULL}`
- [x] 支持 `delta ∈ {0.00, 0.01, 0.02}`
- [x] 根据 `delta` 选择训练标签
- [x] 实现 feature subset 选择逻辑
- [x] 实现 categorical / numeric 特征编码
- [x] 保存模型文件
- [x] 保存 vectorizer / feature_columns
- [x] 保存 `train_summary.json`
- [x] 记录正负样本比例
- [x] 输出目录结构统一到 `outputs/router/models/`

### 输出文件
- [x] `outputs/router/models/xgb_delta_0.01_F4/model.pkl`
- [x] `outputs/router/models/xgb_delta_0.01_F4/feature_columns.json`
- [x] `outputs/router/models/xgb_delta_0.01_F4/train_summary.json`
- [x] （可选）`vectorizer.pkl`

### 验收标准
- [x] 能用 dev features 成功训练
- [x] `train_summary.json` 中记录：
  - [x] `model_type`
  - [x] `feature_set`
  - [x] `delta`
  - [x] `n_train`
  - [x] `positive_rate`
  - [x] `selected_features`
- [ ] XGBoost 首轮默认配置与当前论文 method 保持一致 :contentReference[oaicite:2]{index=2}
- [x] 首轮至少保证 `xgb + F4 + delta=0.01` 能跑通

---

## 2.5 `scripts/eval_router_threshold_scan.py`

### 目标
在 test feature table 上执行 router inference，并对不同 `τ` 做 threshold scan，复现当前论文中的 routing 主表与 threshold 行为。

### 依赖
- 训练好的 router model
- `router_features_test.parquet`
- `router_eval_targets_test.parquet`

### 开发任务
- [x] 明确输入接口：`--model-dir --test-table --eval-targets --taus --out-dir`
- [x] 读取 router 概率输出 `prob_fusion`
- [x] 对每个 `τ` 计算 routed 选择结果
- [x] 计算 `overall_mrr`
- [x] 计算 `fusion_coverage`
- [x] 计算 `gain_precision`
- [x] 计算 `gain_recall`
- [x] 输出 per-query prediction 表
- [x] 输出 threshold scan 汇总表
- [x] 确保输出字段名与当前 threshold 图脚本兼容

### 输出文件
- [x] `outputs/router/eval/router_predictions_xgb_delta_0.01.csv`
- [x] `outputs/router/eval/threshold_scan_xgb_delta_0.01.csv`

### 验收标准
- [x] `threshold_scan_xgb_delta_0.01.csv` 至少包含：
  - [x] `tau`
  - [x] `overall_mrr`
  - [x] `fusion_coverage`
  - [x] `gain_precision`
  - [x] `gain_recall`
- [x] 能与当前 `scripts/plot_threshold_scan_paper_figure.py` 兼容读取 :contentReference[oaicite:3]{index=3}
- [x] 首轮能复现主稿中 `XGBoost @ delta=0.01, tau=0.7` 的主比较口径 :contentReference[oaicite:4]{index=4}

---

## 2.6 `scripts/build_router_master_table.py`

### 目标
构建统一 test-side master table，作为所有 regime / oracle 分析的唯一底表。

### 依赖
- `test_gate_only_query_outcomes.parquet`
- `test_residual_only_query_outcomes.parquet`
- `test_full_model_query_outcomes.parquet`
- `router_predictions_xgb_delta_0.01.csv`

### 开发任务
- [x] 明确输入接口：`--gate-test --residual-test --full-test --router-preds --delta --tau --out`
- [x] 以 `query_id` 对齐所有来源表
- [x] 合并 `rr_gate`
- [x] 合并 `rr_residual`
- [x] 合并 `rr_full_official`
- [x] 合并 `prob_fusion`
- [x] 生成 `select_fusion_tau_07`
- [x] 生成 `rr_routed_xgb_d001_t07`
- [x] 生成 `rr_oracle_gate_residual = max(rr_gate, rr_residual)`
- [x] 生成 `oracle_choose`
- [x] 生成 `rr_gain_gate_vs_residual`
- [x] 生成：
  - [x] `gain_label_d0`
  - [x] `gain_label_d001`
  - [x] `gain_label_d002`
- [x] 生成 router 误差分析字段：
  - [x] `router_correctly_selected_gain_d001`
  - [x] `router_false_positive_d001`
  - [x] `router_false_negative_d001`

### 输出文件
- [x] `outputs/router/eval/router_master_test_xgb_d001_t07.parquet`

### 验收标准
- [x] 每个 query 一行
- [x] 同时保留 routing-compatible 和 official 补充分析所需字段
- [x] `rr_full_official` 单独命名，不与 routing line 混用
- [x] 后续 E1 / E2 / E6 / E7 都只从这张表出发

---

## 2.7 `scripts/analyze_per_regime_winrate.py`  （E1）

### 目标
输出每个 regime 上的 query-level 胜率比较，证明 multimodal gain 的局部性不只是 subgroup MRR 现象，而是真实存在于 query-level outcome。

### 依赖
- `router_master_test_xgb_d001_t07.parquet`

### 开发任务
- [x] 明确输入接口：`--master-table --out`
- [x] 统计 `Gate-only vs Residual-only`
- [x] 统计 `Full Model vs Residual-only`
- [x] 统计 `XGBoost routed vs Residual-only`
- [x] 对每个 regime 计算：
  - [x] `win_rate`
  - [x] `lose_rate`
  - [x] `tie_rate`
  - [x] `mean_rr_diff`

### 输出文件
- [x] `outputs/router/analysis/per_regime_winrate.csv`

### 验收标准
- [x] 每个 regime 至少覆盖：
  - [x] `head_has_img`
  - [x] `head_no_img`
  - [x] `tail_no_img`
- [x] 每个 comparison 一行/多行结构清晰
- [x] 能直接支撑 RQ1 的新增表述

---

## 2.8 `scripts/analyze_per_regime_oracle.py`  （E2）

### 目标
输出每个 regime 的 oracle upper bound 与 learned router gap，证明局部 gain 的上界和当前可利用程度。

### 依赖
- `router_master_test_xgb_d001_t07.parquet`

### 开发任务
- [x] 明确输入接口：`--master-table --out`
- [x] 按 regime 计算：
  - [x] `mrr_residual`
  - [x] `mrr_gate`
  - [x] `mrr_xgb_routed`
  - [x] `mrr_oracle`
  - [x] `oracle_minus_xgb_gap`
  - [x] `oracle_minus_residual_gap`

### 输出文件
- [x] `outputs/router/analysis/per_regime_oracle_upper_bound.csv`

### 验收标准
- [x] Oracle 定义固定为 `max(rr_gate, rr_residual)`
- [x] 不把 `Full Model` 混入 oracle 定义
- [x] 可直接支撑 RQ1 中“局部 gain 上界”的论证

---

## 2.9 `scripts/analyze_regime_routing_behavior.py`  （E6）

### 目标
分析 router 在不同 regime 上的实际行为，证明 selective activation 是 regime-sensitive 的，而不是全局平均提升。

### 依赖
- `router_master_test_xgb_d001_t07.parquet`

### 开发任务
- [x] 明确输入接口：`--master-table --out`
- [x] 按 regime 统计：
  - [x] `fusion_selection_rate`
  - [x] `gain_precision_d001`
  - [x] `gain_recall_d001`
  - [x] `mean_prob_fusion`
  - [x] `mean_rr_routed`

### 输出文件
- [x] `outputs/router/analysis/per_regime_routing_behavior.csv`

### 验收标准
- [ ] 能看出 router 是否在 `head_has_img` 更积极激活 fusion
- [ ] 能看出 router 是否在 `tail_no_img` 更保守
- [ ] 能直接服务 RQ3 的核心句子：
  - [ ] selective activation exploits bounded gain in a regime-sensitive way

---

## 2.10 `scripts/analyze_oracle_gap_decomposition.py`  （E7）

### 目标
分解 Oracle 与 XGBoost router 之间的差距，定位 router 目前最难学的 regime 和错误类型。

### 依赖
- `router_master_test_xgb_d001_t07.parquet`

### 开发任务
- [x] 明确输入接口：`--master-table --out`
- [x] 按 regime 计算：
  - [x] `mrr_oracle`
  - [x] `mrr_xgb_routed`
  - [x] `oracle_gap`
  - [x] `false_negative_rate`
  - [x] `false_positive_rate`
  - [x] `lost_rr_mass_fn`
  - [x] `lost_rr_mass_fp`

### 输出文件
- [x] `outputs/router/analysis/oracle_gap_decomposition.csv`

### 验收标准
- [x] 明确区分：
  - [x] 本该选 fusion 却没选
  - [x] 本该选 residual 却误选 fusion
- [x] 可直接支撑 RQ3 中“router 还差在哪”的分析段落

---

# 3. 首轮建议固定跑通的最小配置

## Router 训练配置
- [x] `model_type = xgb`
- [x] `feature_set = F4`
- [x] `delta = 0.01`

## Threshold scan 配置
- [x] `tau ∈ {0.1, 0.3, 0.5, 0.7, 0.9}`

## 首轮主分析配置
- [x] `router_master_test_xgb_d001_t07.parquet`

> 原因：当前主稿中的最佳 learned router 就是 `XGBoost + delta=0.01 + tau=0.7`，首轮开发先把这一条主线完全打通最稳 :contentReference[oaicite:5]{index=5}

---

# 4. 首轮交付物清单

## A. 原始中间表
- [x] `outputs/router/raw/dev_gate_only_query_outcomes.parquet`
- [x] `outputs/router/raw/test_gate_only_query_outcomes.parquet`
- [x] `outputs/router/raw/dev_residual_only_query_outcomes.parquet`
- [x] `outputs/router/raw/test_residual_only_query_outcomes.parquet`
- [x] `outputs/router/raw/test_full_model_query_outcomes.parquet`
- [x] `outputs/router/raw/dev_relation_priors.csv`

## B. Router 特征与模型
- [x] `outputs/router/features/router_features_dev.parquet`
- [x] `outputs/router/features/router_features_test.parquet`
- [x] `outputs/router/features/router_eval_targets_test.parquet`
- [x] `outputs/router/models/xgb_delta_0.01_F4/model.pkl`
- [x] `outputs/router/models/xgb_delta_0.01_F4/train_summary.json`

## C. Router 评估
- [x] `outputs/router/eval/router_predictions_xgb_delta_0.01.csv`
- [x] `outputs/router/eval/threshold_scan_xgb_delta_0.01.csv`
- [x] `outputs/router/eval/router_master_test_xgb_d001_t07.parquet`

## D. 论文新增分析表
- [x] `outputs/router/analysis/per_regime_winrate.csv`
- [x] `outputs/router/analysis/per_regime_oracle_upper_bound.csv`
- [x] `outputs/router/analysis/per_regime_routing_behavior.csv`
- [x] `outputs/router/analysis/oracle_gap_decomposition.csv`

---

# 5. 首轮完成判定

当且仅当以下条件全部满足，首轮开发才算完成：

- [x] 三个 expert / model 的 query-level outcomes 已统一导出
- [x] relation priors 仅由 dev 构造
- [x] router feature tables 已构建完成
- [x] `xgb + F4 + delta=0.01` 已成功训练
- [x] threshold scan 已生成 CSV，且字段兼容当前论文绘图脚本
- [x] 已生成统一 `router_master_test_xgb_d001_t07.parquet`
- [x] E1 / E2 / E6 / E7 四张分析表已全部输出
- [x] 所有新增分析均未混淆 official line 与 routing-compatible line
- [ ] 结果已足以支撑论文新增段落：
  - [x] RQ1：bounded gain 的 query-level 局部性
  - [x] RQ2：首轮先不扩解释性，但 router 主线已可复现
  - [ ] RQ3：routing behavior 与 oracle gap 的 regime-sensitive 证据

---

# 6. 首轮开发后立即要做的事（不属于本轮实现）

- [ ] 第二轮：`export_xgb_feature_importance.py`
- [ ] 第二轮：`export_logistic_coefficients.py`
- [ ] 第二轮：`summarize_delta_scan.py`
- [ ] 第二轮：统一 paper figure 脚本
- [ ] 第二轮：把新分析表嵌回 `docs/paper/manuscript_main.tex`
- [ ] 第二轮：补论文中对应 caption / discussion / limitation wording

---

# 7. 备注

- [x] 所有 relation prior / label construction 坚持 **dev-only**
- [x] 所有 final routing result 坚持 **test-only reporting**
- [x] `rr_full_official` 只用于 official line 下的 RQ1 补充分析，不与 routing-compatible 主表混算
- [x] 首轮不要再扩模型家族，不要插入新的 base model training
- [x] 首轮目标不是“更多模型”，而是“更完整的证据链”
