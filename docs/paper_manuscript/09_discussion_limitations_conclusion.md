# Retired Reference: Discussion, Limitations, and Conclusion

## Status

This file is an older merged ending draft from the previous analysis-oriented manuscript structure. It is now **reference-only**.

The current latest manuscript is:

- `docs/paper/manuscript_main.tex`

The active conclusion support draft is:

- `docs/paper_manuscript/08_conclusion.md`

The active experiments/discussion support draft is:

- `docs/paper_manuscript/05_experiments.md`

## Current aligned ending claim

The current ending should emphasize:

- bounded multimodal gain under the current OpenBG-IMG protocol;
- failure of naive global-threshold clean routing;
- improvement from direction-specific thresholding to approximately `0.2974` MRR;
- improvement from regression-based gain prediction to approximately `0.2982` MRR;
- paired bootstrap support over the clean rule and `Residual-only`;
- remaining gap to Oracle routing;
- no claim of universal multimodal superiority.

## Do not use this old file as an active ending

Do not copy this older merged ending directly into the current manuscript. Use `08_conclusion.md` and `manuscript_main.tex` instead.
