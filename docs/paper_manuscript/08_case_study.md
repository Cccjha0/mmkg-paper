# Retired Reference: Case Study

## Status

This file is an older case-study draft from the previous analysis-oriented manuscript structure. It is now **reference-only**.

The current latest manuscript is:

- `docs/paper/manuscript_main.tex`

The current TeX manuscript does not maintain a standalone case-study chapter. Case examples may still be reused later, but only if they are checked against the current result line and clean-routing narrative.

## Reusable aligned idea

The following case-level intuition remains compatible with the current paper:

- multimodal-favorable examples tend to appear in image-supported head-side conditions;
- structure-favorable failures tend to appear in image-unavailable or structure-dominant conditions;
- this supports the claim that multimodal gain is local and conditional.

## Current routing-centered interpretation

If case studies are reintroduced, they should support the current paper's routing claim:

> Multimodal evidence should not be activated uniformly. It should be activated selectively when the query is likely to benefit from the fusion expert, and suppressed when the structural expert is more reliable.

## Do not use this old file as an active section

Do not restore this file as a standalone chapter unless the paper is explicitly expanded again.
